version: 1.0
service:
  name: vinyl-shop
  type: app
  runtime: node
  build:
    command: npm install && npm run build
  run:
    command: node server/server.cjs
  ports:
    - port: 3001
      protocol: http
  env:
    - name: NODE_ENV
      value: production